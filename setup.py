from setuptools import setup

setup(name='CSC Data Synthesizer',
      version='0.1',
      description='Shared module for generating children services data',
      author='Social Finance',
      packages=['cscsynth'],
      zip_safe=False)